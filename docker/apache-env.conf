# Pass container-level env vars through to mod_php so Symfony picks them up
# instead of falling back to .env (which is checked into the repo for dev).
PassEnv APP_ENV
PassEnv APP_DEBUG
PassEnv APP_SECRET
PassEnv DATABASE_URL
PassEnv DEFAULT_URI
PassEnv APP_SHARE_DIR
